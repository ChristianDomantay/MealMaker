<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="StartBtn">
        <item name="android:background">@drawable/mybutton</item>
        <item name="android:colorBackground">@color/REDprimaryColor</item>
    </style>

    <style name="mealMaker">
        <item name="android:fontFamily">@font/sourcesanspro</item>
    </style>

    <style name="editTxt">
        <item name="android:background"> @null </item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="ListOfIngredients">
        <item name="android:fontFamily">@font/segoeui</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/MaroonPrimaryColor</item>
        <item name="android:textSize">20dp</item>
    </style>
    
    <style name="searchButton">
        <item name="android:fontFamily">@font/sourcesanspro</item>
        <item name="android:textSize">16dp</item>
        <item name="android:textStyle">normal</item>
        <item name="android:background">@drawable/roundedbutton</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textAlignment">center</item>

    </style>

<style name="recipeList">
    <item name="android:textColor">@color/white</item>
    <item name="android:textSize">25dp</item>
    <item name="android:fontFamily">@font/sourcesanspro</item>
    <item name="android:textAlignment">center</item>

</style>
    
    <style name="imagebtn">
        <item name="android:background">@null</item>
    </style>
</resources>
